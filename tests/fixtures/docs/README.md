# Fixture docs

Minimal Markdown fixture used by the workflows-library e2e pipeline to exercise
`ci-docs` (markdownlint).

- one list item
- another list item

```bash
echo "code fence for good measure"
```
